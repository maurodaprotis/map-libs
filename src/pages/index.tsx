import { Layout } from "@/components/Layout";

export default function Home() {
  return (
    <Layout title="React Map Libraries">
      <h2 className="mx-auto w-fit font-medium text-gray-500">
        ← Choose one of the options
      </h2>
    </Layout>
  );
}
